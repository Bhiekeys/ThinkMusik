import Reusing from './Reusing';

const Learning = () => {
  return (
    <Reusing
      type="Continue learning"
      paragraph="Browse courses to start learning."
      head="You don’t have any active courses"
    />
  );
};
export default Learning;
